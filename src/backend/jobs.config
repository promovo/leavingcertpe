/{
  "jobs": [
    {
      "functionLocation": "/maintenance.js",
      "functionName": "runDailyAccessMaintenance",
      "description": "Queue LeavingCertPE renewal reminders and expire access after the fixed academic-year end date.",
      "executionConfig": {
        "cronExpression": "15 2 * * *"
      }
    }
  ]
}